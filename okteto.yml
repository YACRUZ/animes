build:
 yacruz:  # cambiar por su namespace de okteto
   image: okteto.dev/yacruz:latest  # cambiar por su namespace de okteto

deploy:
 - okteto build # build the image with the latest code
 - kubectl apply -f manifests # create all the resources in kubernetes
dev:
 adsoftsito:
   command: bash
   sync:
   - .:/caps   # cambiar por su proyecto